language: c
install: wget https://raw.githubusercontent.com/ocaml/ocaml-ci-scripts/master/.travis-docker.sh
script: bash -ex .travis-docker.sh
services:
- docker
env:
  global:
  - PINS="mastodon-archive-viewer:."
  - DISTRO="debian-stable"
  matrix:
  - PACKAGE="mastodon-archive-viewer" OCAML_VERSION="4.02.3" DEPOPTS="ffmpeg"
  - PACKAGE="mastodon-archive-viewer" OCAML_VERSION="4.03.0" DEPOPTS="ffmpeg"
  - PACKAGE="mastodon-archive-viewer" OCAML_VERSION="4.04.2" DEPOPTS="ffmpeg"
  - PACKAGE="mastodon-archive-viewer" OCAML_VERSION="4.05.0" DEPOPTS="ffmpeg"
  - PACKAGE="mastodon-archive-viewer" OCAML_VERSION="4.06.0" DEPOPTS="ffmpeg"
